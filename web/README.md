# JobRadar Web

Frontend React + Vite + Tailwind (tour de contrôle).

Repo backend séparé : `../api`

## Setup

```bash
cp .env.example .env
# VITE_API_URL=http://localhost:4000

npm install
npm run dev
```

App : http://localhost:5173

L'API doit tourner en parallèle dans le repo `../api`.
